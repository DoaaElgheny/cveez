export enum Payment {
  NotPaied = 0,
  Paied = 1,
  Expired = 2,
}

export enum ConditionType {
  ShowLogo = 1,
  ShowOpportunity = 2,
  ApplyOpportunity = 3,
  ShowRequests = 4,
  OpportunityNumber = 5,
  CveezDownload = 6,
}

export enum ConditionValue {
  None = 0,
  Integer = 1,
}
